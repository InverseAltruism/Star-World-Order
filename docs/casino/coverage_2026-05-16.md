# Casino Contract Coverage — Verification (2026-05-16)

**Task:** `[SWO_CASINO_FORGE_COVERAGE_VERIFY_2026-05-16]` — closes the loop on
`[SWO_CASINO_TEST_COVERAGE_BAR]`, which was marked `[x] [UNVERIFIED]` on
2026-05-16T05:37 with no on-disk evidence (`docs/casino/` lacked any coverage
artifact). This document re-runs the actual coverage workflow and records the
result against the claimed **90% line / 80% branch** floor.

## How this was produced

```bash
cd contracts/casino
forge coverage --report lcov --report-file coverage/lcov.info --ir-minimum
# 14 test suites, 198 tests passed, 0 failed
```

- **Tooling:** `forge 1.6.0-v1.7.0` (commit `f83bad9`).
- **`--ir-minimum`** is required for this codebase — without it `forge coverage`
  hits the "stack too deep" compile error. This matches the verified command
  recorded for `[SWO_CASINO_CI_FORGE_COVERAGE]`. Note that IR-minimum coverage
  can under-count slightly versus a full optimizer build, so figures near the
  bar should be treated as a conservative lower bound.
- Raw report: `contracts/casino/coverage/lcov.info` (non-empty, 1662 lines).

## Per-contract coverage

Bar: **line ≥ 90%**, **branch ≥ 80%**. A contract passes only if it meets **both**.

| Contract                 | Line %  | Lines (hit/found) | Branch % | Branches (hit/found) | Func %  | Verdict  |
|--------------------------|---------|-------------------|----------|----------------------|---------|----------|
| `CasinoAllowlist.sol`    | 100.0%  | 24/24             | 100.0%   | 3/3                  | 100.0%  | **PASS** |
| `CosmicFlip.sol`         | 94.6%   | 70/74             | 85.7%    | 18/21                | 90.9%   | **PASS** |
| `ConstellationClimb.sol` | 92.5%   | 111/120           | 80.0%    | 32/40                | 93.8%   | **PASS** |
| `GravityDice.sol`        | 89.9%   | 71/79             | 86.4%    | 19/22                | 91.7%   | **FAIL** |
| `CasinoBankroll.sol`     | 88.2%   | 97/110            | 75.0%    | 21/28                | 100.0%  | **FAIL** |

Supporting contract (not part of the named bar, recorded for context):

| Contract                      | Line %  | Lines | Branch % | Branches | Func % |
|-------------------------------|---------|-------|----------|----------|--------|
| `CommitRevealRandomness.sol`  | 100.0%  | 9/9   | 0.0%     | 0/2      | 100.0% |

## Verdict

**The claimed 90% line / 80% branch bar is REFUTED.** Three of five named
contracts pass; two fall below the floor:

- **`GravityDice.sol` — FAIL.** Line 89.9% (< 90%, short by ~1 line). Branch
  86.4% passes. A single additional covered line clears the bar.
- **`CasinoBankroll.sol` — FAIL.** Line 88.2% (< 90%) **and** branch 75.0%
  (< 80%). This is the weakest contract on both axes — the bankroll accounting
  has 13 uncovered lines and 7 uncovered branches.

`CasinoAllowlist.sol`, `CosmicFlip.sol`, and `ConstellationClimb.sol` meet the
bar on both line and branch.

## Follow-up test gaps (per the original "any contract below the bar gets a
follow-up row" requirement)

`QUEUE.md` is not part of this repository (it is an orchestration artifact
maintained outside the workspace), so the follow-up rows are recorded here for
the Phase B queue to ingest:

- **`[SWO_CASINO_TEST_GRAVITYDICE_GAP_2026-05-16]`** — Raise `GravityDice.sol`
  line coverage from 89.9% → ≥ 90% (8 uncovered lines of 79). Add unit cases for
  the uncovered settlement/refund paths in `test/GravityDiceUnit.t.sol`.
- **`[SWO_CASINO_TEST_CASINOBANKROLL_GAP_2026-05-16]`** — Raise
  `CasinoBankroll.sol` line 88.2% → ≥ 90% (13 uncovered lines) **and** branch
  75.0% → ≥ 80% (7 uncovered branches). Target the uncovered guard branches in
  the deposit/withdraw/solvency accounting; extend
  `test/CasinoBankrollUnit.t.sol` and `test/CasinoBankrollBreaker.t.sol`.

Both rows must be filed before T1 per the original spec.
